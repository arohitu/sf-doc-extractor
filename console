// Copy/Paste this into Console to find the content
(function probe(root = document.body, depth = 0) {
    if(depth > 10) return; // safety break
    
    // Check for our target
    if(root.querySelector && root.querySelector('doc-xml-content')) {
        console.log(`%c FOUND IT at depth ${depth}`, 'color: green; font-size: 14px; font-weight: bold;');
        console.log(root.querySelector('doc-xml-content'));
        return;
    }

    // Check all children for shadow roots
    const walker = document.createTreeWalker(root, NodeFilter.SHOW_ELEMENT);
    while(walker.nextNode()) {
        if(walker.currentNode.shadowRoot) {
            console.log(`Checking shadow root of: ${walker.currentNode.tagName}`);
            probe(walker.currentNode.shadowRoot, depth + 1);
        }
    }
})();

If the console prints **FOUND IT**, my new code above will work.
If it prints nothing or errors, Salesforce might have changed the tag name to something else (like `dx-article` or `bridge-app`), and we will need that tag name to update the selector.